# Carefloor Release Authority

Protected, candidate-independent approval and promotion authority for BrainVI
Carefloor. One reusable workflow signs an exact frozen candidate after GitHub
environment approval. The promotion workflow separately validates the immutable
release envelope and staged Vercel deployment before moving the canonical alias.
